"""RESERVADO para C-03: Autenticación, JWT, Argon2id, AES-256.

Este módulo se completa en C-03 con:
- Generación y verificación de JWT (access + refresh rotation).
- Hashing de contraseñas con Argon2id.
- Cifrado/descifrado AES-256 para PII (CBU, DNI) y secretos.
- ``verify_password``, ``hash_password``, ``create_access_token``,
  ``verify_token``, ``encrypt_value``, ``decrypt_value``.
"""
